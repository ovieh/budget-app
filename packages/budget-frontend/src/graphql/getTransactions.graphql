query getTransactions {
  getTransactions {
    id
    transactionDate
    creditAmount
    debitAmount
    balance
    transactionDescription
    category {
      id
      name
      budget
    }
  }
}